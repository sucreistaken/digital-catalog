events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    server {
        listen 80;
        server_name localhost plastime.com.tr www.plastime.com.tr freegardensaksi.com www.freegardensaksi.com;
        root /usr/share/nginx/html;
        index index.html;

        # React Router için - tüm route'ları index.html'e yönlendir
        location / {
            try_files $uri $uri/ /index.html;
        }

        # API proxy (backend'e yönlendir) - no caching for dynamic data
        location /api {
            proxy_pass http://backend:3001;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            add_header Cache-Control "no-store, no-cache, must-revalidate";
        }

        # Uploads proxy - short cache so updated images reflect quickly
        location ^~ /uploads {
            proxy_pass http://backend:3001;
            proxy_set_header Host $host;
            expires 5m;
            add_header Cache-Control "public, max-age=300";
        }

        # Cache static assets

        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}
